import sqlite3\n\nclass Database:\n    def __init__(self, db_name='user_data.db'):\n        self.connection = sqlite3.connect(db_name)\n        self.create_tables()\n\n    def create_tables(self):\n        with self.connection:\n            self.connection.execute('''CREATE TABLE IF NOT EXISTS user_info (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                username TEXT NOT NULL,\n                high_score INTEGER DEFAULT 0,\n                created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n            )''')\n\n            self.connection.execute('''CREATE TABLE IF NOT EXISTS game_progress (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                user_id INTEGER,\n                level INTEGER DEFAULT 1,\n                progress TEXT,\n                FOREIGN KEY(user_id) REFERENCES user_info(id)\n            )''')\n\n    def add_user(self, username):\n        with self.connection:\n            self.connection.execute('INSERT INTO user_info (username) VALUES (?)', (username,))\n\n    def update_high_score(self, user_id, score):\n        with self.connection:\n            self.connection.execute('UPDATE user_info SET high_score = ? WHERE id = ?', (score, user_id))\n\n    def save_progress(self, user_id, level, progress):\n        with self.connection:\n            self.connection.execute('INSERT INTO game_progress (user_id, level, progress) VALUES (?, ?, ?)', (user_id, level, progress))\n\n    def get_user_info(self, user_id):\n        cursor = self.connection.cursor()\n        cursor.execute('SELECT * FROM user_info WHERE id = ?', (user_id,))\n        return cursor.fetchone()\n\n    def close(self):\n        self.connection.close()\n\n# Example usage\nif __name__ == '__main__':\n    db = Database()\n    db.add_user('test_user')\n    db.update_high_score(1, 100)\n    db.save_progress(1, 1, 'completed')\n    print(db.get_user_info(1))\n    db.close()